# HydroCore-v5 M11.5 frozen-finalist full-validation protocol

Status: frozen before M11.5 results. This is the full non-locked validation of
the M11.2-finalist identity, not selection, tuning, M11.3/M11.4 work, or a
locked evaluation. M11.3 and M11.4 are intentionally unused/reserved in this
governed sequence: their relevant responsibilities were covered by M10 system
validation, M10.5 serving freeze, and M11.2 identity freeze.

## Fixed parent identity and no-lock rule

The sole subject is the M11.2 `HydroCore-v5 M10 frozen release` finalist at
`models/hydrocore-v5-release`: seed `20260814`, checkpoint
`de2b3f56243a1933d1d7c5957cd74a29fade119f7d104ce7f1500b3dd7b6d2a5`,
release manifest
`f3fb08642738128f020c50e20e6b68c417bf80703f7ef6bc8f42db2aa41f8d34`,
calibration
`8f77f06b72316455e1f8040dbeb5907503e4eb623dd527d9ea809a56e96c046d`,
and calibration artifact
`f2503e856c467eb38c6c7f6dbde679527c1921925941ec52809bd6e8e6dd16dd`.
M11.1 protocol SHA is
`52d911b86b37c7de095643cf02415601e5e1b198cf17c849239b97da8e94264d`;
the M11.2 certificate remains authoritative.

Before every matrix row, the runner verifies this exact certificate identity.
Any drift closes `M11_5_BLOCKED_IDENTITY_DRIFT`. No locked source may be
read, enumerated, hashed, or authorized; locked status is asserted false
before and after. M11.5 cannot authorize M11.6.

## Matrix and evidence strategy

The matrix has rows A--N: identity/reproducibility; predictive localization;
calibration/actionability; robustness/sensor quality; development OOD/topology;
deterministic Scout; deterministic planning/exact WNTR; human approval/no
actuation; end-to-end trajectory; serving/release parity; fail-closed behavior;
output governance; known limitations; and software/release quality.

Rows that would duplicate an already-closed scientific campaign use
`CLOSED_EVIDENCE_REUSE`, only after exact checkpoint/calibration/release and
serving-semantics provenance is mechanically verified. Identity, clean-load,
and release checks are `FRESH_FROZEN_FINALIST_RUN`. No fresh incident
population is generated by this protocol, so no new seed namespace is used.

Hard gates reuse only pre-existing requirements: exact identity parity;
M9/M10 frozen predictive/calibration gates; finite outputs; zero authority and
safety counters; deterministic fallback; WNTR/EPANET final authority; human
approval/no-actuation; release fail-closed/no-v4-fallback; and established
software gates. Metrics without a prior acceptance threshold are descriptive.
No post-hoc numeric threshold, re-run seed, repair, calibration fit, threshold
change, or policy/feature/model change is permitted.

## Frozen authority and limitations

The runtime remains sentinel-only with the allowlist `event_cause`,
`event_presence`, `evidence_sufficiency`, `relative_strength`, and
`source_node`; `next_step` remains `SUPPRESSED_UNSUPERVISED`. Deterministic
`OODDetector`, `rank_sample_locations`, `generate_response_plans`, exact
WNTR/EPANET, and human approval remain authoritative. Learned OOD, Scout, and
Strategist remain non-authoritative; autonomous actuation remains absent.

The M10.4 vacuous NO_ACTION Gate E, modest-but-no-action-changing sampling
benefit, limited development unseen-topology evidence, retained M9.6-fixed vs
M10.4-incident-elapsed age-semantic difference, and non-promotion of learned
OOD/Scout/Strategist are limitations, not repaired or hidden here.

## Closure and readiness

Success is `M11_5_FULL_VALIDATION_PASS`; a completed hard-gate failure is
`M11_5_FULL_VALIDATION_FAIL`; identity drift is
`M11_5_BLOCKED_IDENTITY_DRIFT`; unexecutable validation is
`M11_5_BLOCKED_EXECUTION`; governance/locked-data failure is
`M11_5_BLOCKED_GOVERNANCE`.

Only a green matrix may state M11.6 prerequisites are satisfied. Even then it
must record `locked_evaluation_authorized=false`,
`locked_test_opened=false`, and `awaiting_explicit_human_authorization=true`.
The required stop boundary is: **M11.5 COMPLETE. M11.6 PRECONDITIONS
SATISFIED. LOCKED EVALUATION IS NOT AUTHORIZED. STOPPING FOR EXPLICIT HUMAN
AUTHORIZATION.**

# Experiments

Each run should use a unique directory containing the resolved configuration, environment
snapshot, git revision, random seeds, dataset-manifest digest, metrics JSON, and immutable
checkpoint references. Generated runs are excluded from version control; compact measured
summaries belong under `reports/results`.
